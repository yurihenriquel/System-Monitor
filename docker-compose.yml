services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env

  collector:
    build:
      context: .
      dockerfile: Dockerfile.collector
    depends_on:
    - api